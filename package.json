{
  "name": "vector-td-enhanced",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.87.0"
  },
  "devDependencies": {
    "gh-pages": "^6.1.1",
    "typescript": "^5.4.0",
    "vite": "^5.2.0"
  }
}
